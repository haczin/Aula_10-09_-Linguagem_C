#include <stdio.h>
int main()

{
    int A;
    int B;
    int temp;

    A = 5;
    B = 10;
    temp;

    printf("Antes da troca A = %d, B = %d\n", A, B);

    temp = A; 
    A = B;
    B = temp;
    printf("Depois da troca: A = %d, B = %d\n", A, B);

}
